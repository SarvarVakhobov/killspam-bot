# Seed spam patterns. One regex per line, case-insensitive.
# Edit freely — reloaded on bot start and after every admin-taught addition.
# Lines starting with # are comments. [section] headers group by category.
#
# SCOPE: the bot blocks 18+ content (the AI layer, the local NudeNet photo scan
# and the explicit-link bio check in core/patterns.py), phishing links and app
# downloads (core/linkcheck.py) and link floods (handlers/spam_detector.py).
# Nothing in this file blocks automatically any more. The [ads] rules that lived
# here (private t.me/+ invite links, crypto-airdrop wording) muted real members
# for listing their own channel in their bio, so they were removed. Do NOT add
# non-adult rules back; a group admin can still teach a narrow keyword for their
# own group with /teach.

[ads]
